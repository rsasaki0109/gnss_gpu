"""ROS2 integration for gnss_gpu.

This subpackage provides ROS2 nodes for GPU-accelerated GNSS positioning.
ROS2 (rclpy) is only required at runtime when launching nodes, not for
importing the gnss_gpu library itself.
"""

__version__ = "0.1.0"
