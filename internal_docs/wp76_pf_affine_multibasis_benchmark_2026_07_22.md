# WP76 affine multi-basis promotion

WP76 fixes the carrier common-translation gauge with the accepted static
anchor beginning at epoch 3811. The moving correction is a three-dimensional
affine state equal to its fitted reference offset at epoch 3666 and exactly
zero at the accepted boundary. GSI constrains final Up, while a fixed 49-cell
East/North grid supplies integer basins independently for carrier-reference
ranks 0, 1, and 2.

The truth-free selector retains absolute CP/PR gates, requires a three-basis
cluster within 0.12 m, then ranks clusters independently by calibrated OSM
road-band violation and summed carrier RMS. Candidate 27 agrees across all
three bases within 0.072 m, ranks 1/2, and beats its runner by 33.3%.

The adjacent Nagoya transfer has only one cluster and the Tokyo unsafe holdout
has none, so both fail closed before truth audit. The hash-recomputing promoter
recreates the target decision and verifies both rejection artifacts plus the
unchanged M4 hashes.

The production full-denominator audit gains all 55 target epochs with zero
loss, moving Nagoya run1 from 5,061/7,583 (66.7414%) to 5,116/7,583
(67.4667%). FIX and false FIX remain zero; runtime FGO remains disabled.
